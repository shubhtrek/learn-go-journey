package main

// TODO: Implement a function that executes unsafeAction (a function argument).
// If unsafeAction panics, recover from it and return the panic message as a string.
// If it does not panic, return "success".
func RunSafely(unsafeAction func()) string {
	// Write your code here
	return ""
}
