# Project 1: Concurrent URL Health Checker 🛠️

Build a command-line tool that takes a list of website URLs and checks if they are online.

## Requirements
1. Must use `goroutines` to check multiple sites at the same time.
2. Must use `channels` or `sync.WaitGroup` to sync output.
3. Print response times and HTTP statuses.
